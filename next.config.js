/** @type {import('next').NextConfig} */
const nextConfig = {
  // React strict mode
  reactStrictMode: true,

  // Turbopack with minimal config (empty object satisfies the requirement)
  turbopack: {},
};

module.exports = nextConfig;